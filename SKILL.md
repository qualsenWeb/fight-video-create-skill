---
name: combat-storyboard
description: 根据用户提供的人物、参考资料、故事梗概或简单剧本，渐进检索打斗资料库，生成可拍摄的视频剧本、动作设计和分镜表。用于打斗视频策划、动作短片、武侠/枪战/奇幻/机甲/巨兽等战斗分镜，以及将资料文件夹新增样本增量吸收到 Skill references。
---

# 打斗视频剧本与分镜

## 核心原则

- 使用渐进式检索：先读索引，再读公共规范，然后只读与当前任务匹配的专用模板和题材 reference。
- 禁止一次性读取全部 references 或全部 `资料/` 样本。
- 用户事实高于资料范例；参考只提供动作结构、镜头方法和题材语法，不得覆盖角色、剧情、胜负和世界观。
- 输出必须从“简单剧本”演化为可执行的视频剧本与分镜，不只返回提示词堆砌。

## 默认工作流

### 1. 建立最小简报

提取：

```text
Brief = Characters + Goal + Conflict + Setting + Style + Duration + AspectRatio + Assets + Outcome + TargetTool
```

仅在缺失信息会改变故事结构时提问。用户未指定时：根据素材推断媒介，时长按剧本容量合理拆段，画幅使用 16:9，不虚构目标工具能力。

### 2. 渐进检索

严格按以下顺序：

1. 必读 [references/00-reference-index.md](references/00-reference-index.md)。
2. 必读 [references/01-core-guidelines.md](references/01-core-guidelines.md)。
3. 从索引中选择 **最多 1 个专用模板**；没有明确需要则不读。
4. 选择 **1 个主题材 reference**；确有混合题材时最多再读 1 个辅题材 reference。
5. 只有用户要求角色卡、选招、完整动作知识库或详细镜头规格时，才读 [references/02-template-combat-library.md](references/02-template-combat-library.md)，必要时再定向读取原始 `资料/武打skill.md` 的相关章节，禁止全量读取。

单次创作通常读取 2—4 个 reference，不得为了“更全面”遍历全部文件。

### 3. 匹配而非套用

为输入建立标签：

```text
Match = CombatMechanism + Scale + Medium + Space + AssetMode + Rhythm
```

匹配优先级：战斗机制 > 空间尺度 > 输入素材模式 > 媒介风格 > 表面题材。只复用与当前动作因果有关的骨架，不直接复制参考角色、招式名称、颜色或结局。

### 4. 生成视频剧本

将用户简单剧本扩展为：

- 一句话戏剧目标；
- 起始状态；
- 冲突升级；
- 情绪或战术拐点；
- 终结结果与余波；
- 角色、道具、空间和伤损的连续状态。

每个段落都必须推动胜负、关系、目标或空间状态，禁止纯展示镜头占用篇幅。

### 5. 生成分镜表

每镜至少包含：

```text
镜号 | 时间 | 景别/机位 | 画面与动作 | 运镜 | 声音 | 状态交接
```

要求：

- 动作写成攻击—防守/受击—位移—结果链；
- 镜头说明观察位置、跟随目标和停止位置；
- `状态交接` 记录人物位置、朝向、伤损、武器、动作惯性和环境变化；
- 长剧本按可独立生成的视频片段分组，每组有明确首帧和尾帧状态。

### 6. 交付结构

默认输出：

```markdown
# 创作定位
## 故事核心
## 参考匹配

# 视频剧本
## 角色与连续性锚点
## 分段剧本

# 分镜表
[逐镜内容]

# 生成注意事项
[仅列对当前项目有效的约束]
```

`参考匹配` 只说明使用了哪些资料类别及其作用，不展示内部推理。

## 质量门槛

交付前检查：

- 剧情是否有目标、升级、拐点和结果？
- 每次攻击是否有对象、方向、防守/受击和状态变化？
- 镜头是否能看清关键动作，是否出现无理由越轴或重复运镜？
- 每镜是否继承上一镜的角色位置、武器、伤损和运动方向？
- 特效是否附着于肢体、武器、能力或命中点？
- 是否只使用目标工具真实支持的素材和格式？

不满足时先修复上游剧本或动作链，再修改镜头，禁止用更多形容词掩盖结构问题。

## 增量吸收新样本

当用户要求“更新资料”“吸收新增样本”“同步 references”或表达等价意图时：

1. 读取 [references/source-registry.md](references/source-registry.md)。
2. 列出项目 `资料/` 下的文档；排除已登记文件、Skill 自身和临时/生成目录。
3. **只读取未登记的新文件**，不得重读现有样本。
4. 判断新样本属于公共规则、专用模板或一个主/辅题材。
5. 去重后把新增知识追加到对应 reference；若只提供同构案例，登记其变体价值，不复制整段。
6. 更新 `source-registry.md` 的文件名、类别、吸收位置和摘要。
7. 若新增类型现有索引无法覆盖，先扩展索引，再新增一个一级 reference；不要建立多层 reference。
8. 完成后报告新增数量、归类位置、合并项和未吸收原因。

已登记样本视为不可变基线；除非用户明确要求重建，不重新摘要、不批量校验旧文件。